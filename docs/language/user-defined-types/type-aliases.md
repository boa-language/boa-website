# Type aliases

Use the `type` keyword to create a type alias, which is basically another name for the same type. There is no distinction between the original type and its aliases; they are interchangeable.

## Simple example

```boa
type NewType = AlreadyExistingType
```
